name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Lint, test, build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Build
        run: npm run build
        env:
          # Next.js build needs these to be set; real values are not used at build time.
          DATABASE_URL: postgresql://adventure:adventure@localhost:5432/adventure
          BETTER_AUTH_SECRET: build-time-placeholder-secret-at-least-32-chars
          BETTER_AUTH_URL: http://localhost:3001
          AWS_REGION: us-east-1
          AWS_ACCESS_KEY_ID: build-placeholder
          AWS_SECRET_ACCESS_KEY: build-placeholder
          BEDROCK_NARRATIVE_MODEL_ID: us.anthropic.claude-sonnet-4-20250514-v1:0
          BEDROCK_IMAGE_MODEL_ID: amazon.nova-canvas-v1:0
          STRIPE_SECRET_KEY: sk_test_build_placeholder
          STRIPE_WEBHOOK_SECRET: whsec_build_placeholder
